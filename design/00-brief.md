# UI1 brief

Deliver a static landing-site builder that can be used without a
consumer-specific frontend project. It accepts either legacy README/changelog
files, a TypeScript custom adapter, or a versioned site model loaded through
`subzerodev-data-json`. Preserve an explicit custom adapter seam for existing
React sites. No product content, hosting policy, analytics, JSON authoring
tools, or consumer migration belongs here.
